# Start Nimbus with verbose output
nimbus:
  command:
    - /bin/bash
    - -c
    - |
      echo "Starting Nimbus with STORM_CONF_DIR=/conf"
      export STORM_CONF_DIR=/conf
      echo "Running storm nimbus..."
      exec storm nimbus